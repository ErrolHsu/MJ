<div class="mt">
	<%= form_tag(perfect_info_path, method: "post") do %>
		<%= label_tag :name %>
		<%= text_field_tag(:name, nil, class: "form-control") %>
		<%= submit_tag("送出", class: "btn btn-1 mt") %>
	<% end %>
</div>

<hr>